{
  "name": "opscopilot-k8s-bot-demo",
  "version": "0.2.0",
  "description": "Local, credential-free AI Kubernetes operations assistant demo — Llama 3 (via Ollama) as the reasoning model, k8sgpt-style AI diagnosis, and mock cluster data. No cluster, kubeconfig, or Teams connection required.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "license": "MIT"
}
